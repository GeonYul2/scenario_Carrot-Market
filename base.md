📋 Project Blueprint: Daangn Alba CRM Reactivation Analysis1. 프로젝트 배경 및 페르소나분석가 페르소나: 당근(Daangn) 마케팅팀의 CRM Specialist. SQL 기반의 데이터 추출과 비즈니스 지표 개선을 목표로 함.핵심 과제: '정산 완료' 경험이 있는 고관여 유저가 이탈 징후를 보일 때, 과거 이력 기반의 **개인화된 공고 추천(유사 업종 + 더 높은 시급)**을 통해 복귀(Reactivation) 및 재지원(Apply)을 유도함.2. 데이터 스키마 정의 (Schema Definition)CLI는 아래 5개 테이블 구조를 기반으로 CSV를 생성하고 SQL을 작성해야 함.테이블명핵심 컬럼 및 설명usersuser_id, region_id, push_on(0/1), total_settle_cnt(정산 횟수)settlementsst_id, user_id, category_id, settled_at, final_hourly_rate(정산받은 시급)job_postsjob_id, category_id, region_id, hourly_rate(현재 시급), posted_atcategory_maporiginal_cat, similar_cat (예: 카페 -> 서빙, 베이커리 등 유사 업종 매핑)campaign_logslog_id, user_id, ab_group(Control/A/B), is_applied(0/1), sent_at3. 데이터 생성 규칙 (Python Generation Logic)CLI에게 다음 로직을 반영한 data_generator.py 생성을 요청함.IQR 분포 주입: total_settle_cnt가 높은 유저(프로 알바러)는 재방문 주기가 짧고, 낮은 유저는 주기가 길게 분포하도록 settled_at 데이터를 생성할 것.인과관계 설정: * Variant B (Personalized): 유사 업종(similar_cat)이면서 시급(hourly_rate)이 과거 정산액(final_hourly_rate)보다 높은 공고가 매칭된 경우, is_applied 확률이 가장 높게 설정.Variant A (Generic): 일반 복귀 메시지는 중간 확률.Control (Holdout): 자연 복귀율(가장 낮은 확률) 반영.4. 분석 시나리오 및 SQL 요구사항 (Analysis Logic)[STEP 1] Segmented IQR 기반 이탈자 정의total_settle_cnt 그룹별로 유저의 Recency(마지막 정산 후 경과일) 분포를 계산.$$Limit = Q3 + (1.5 \times IQR)$$ 공식을 사용하여 통계적 이상치(이탈자)를 타겟팅하는 쿼리 작성.[STEP 2] 개인화 매칭 로직 (Recommendation logic)타겟 유저의 과거 category_id를 category_map과 조인하여 유사 업종 공고를 추출.현재 공고의 hourly_rate가 유저의 마지막 final_hourly_rate보다 큰 공고만 필터링.[STEP 3] 3-Arm AB Test 성과 측정각 그룹별(Control, A, B) 지원 전환율($Apply\ Rate$) 계산.Uplift 측정: Control 대비 Variant B의 증분 성과 및 통계적 유의성(Chi-square test 등) 확인 로직 포함.[STEP 4] 장기 코호트 분석 (Retention)캠페인 발송 주차(Week 0)를 기준으로 복귀한 유저들이 Week 1, Week 2, Week 4에 다시 접속하거나 지원하는지 추적하는 쿼리 작성.5. CLI에게 내리는 최종 명령 (Action Prompt)"위 설계 문서를 바탕으로 다음 작업을 수행하라.VS Code 환경에서 실습 가능한 Python 가상 데이터 생성 스크립트를 작성하라. (실제 당근알바의 비즈니스 로직이 반영된 분포여야 함)생성된 CSV를 기반으로 IQR 계산 -> 타겟 추출 -> AB 테스트 성과 분석 -> 코호트 추적으로 이어지는 단계별 SQL 파일을 생성하라.모든 분석은 XAMPP(MySQL/MariaDB) 환경에서 실행 가능한 문법을 사용할 것."이 문건에서 보완하거나 특정하고 싶은 세부 수치(예: 전체 유저 수, 특정 지역 코드 등)가 있으신가요? 없으시다면 바로 CLI에 적용할 준비가 끝난 상태입니다. Would you like me to adjust any of the technical specifics before you hand this over to the CLI?